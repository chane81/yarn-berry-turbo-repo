# turbo repo(monorepo) + yarn berry

# command

```bash
  npx create-turbo@latest (select yarn)
  yarn set version stable

  # edit .yarnrc.yml
  nodeLinker: pnp

  yarn install
  yarn dev
  yarn build
```